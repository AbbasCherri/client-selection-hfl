# Results Provenance

**Rule: every number, table, and figure in the paper must trace to a row
in this file.** Each results-producing run gets one row: when it ran,
the exact command/config, what it wrote, the git commit of the code that
produced it, and the paper table/figure it feeds. A number without a row
here does not go in the paper; a row whose attribution is uncertain is
marked "TODO: confirm" rather than guessed. Add a new row *before*
quoting anything from a new run. (Per-run ground truth lives next to the
outputs: every harness writes its resolved config YAML and a
`seed_manifest.csv` before the run starts.)

## Current state (2026-08-09): everything below is VOID pending the v5 rebuild

**No row in the table below is paper material right now.** Two independent
defects, both discovered 2026-08-08/09, invalidate every run that precedes
them. Neither is visible in any output file, which is why they survived so
long — every affected run produced complete, well-formed, plausible-looking
results.

**Defect 1 — incoherent flight geometry (affects every run).** The UAV altitude
band was 20-120 m while `R_comm` ran to 20 km. Under the Al-Hourani channel the
radius-vs-altitude curve peaks at a fixed elevation angle (20.34° suburban), so
the coherent ground radius is `z/tan(θ_opt)` = 54-324 m for that band. Every
configured radius above ~324 m pinned the vertical optimum at a bound, and at
20 km the elevation angle is 0.34°, giving P(LoS) ≈ 3% — a 97% NLoS link, i.e.
an aerial base station with its defining advantage switched off. `LinkModel`
never objected because it back-solves whatever path-loss budget the configured
`R_comm` requires. Corrected to a 100-2000 m band at `R_comm` = 5 km (FL) and
100-400 m at 500 m (Tier-1), both interior to the coherent interval, guarded by
`tests/sanity_checks/check_altitude_band.py`.

**Defect 2 — per-UAV capacity below the level at which the model can learn
(affects the FL runs).** Measured 2026-08-09 in `results/probe_topology`
(6 cells × 3 seeds, R = 5 km, 100 rounds): capacity ≤ 3 rises to ~0.34 macro-F1
by round 10 and then *unlearns* monotonically to the constant-predictor floor
(cap=1 ends at 0.199, below it), while capacity ≥ 4 recovers and climbs.
Capacity orders the outcome exactly; coverage does not order it at all — the
best cell has the lowest coverage in the grid (82.3%) and the worst has 85%.
Cause: with `fusion_owner: uav` each UAV trains img_proj+fusion on the pooled
shard of its assigned clients, so capacity is that shard's width, and Noto
damage classes are geographically clustered enough that a 2-client shard inside
one 5 km disc is near single-class. Now observable per round as
`shard_minority_share` / `shard_class_entropy`.

**A retired claim, recorded so it is not reintroduced.** An earlier diagnosis
held that the task needs ~120 clients training per round. It came from a single
observation, confounded participation with radius, and is falsified: the cap=1
cell selects 120 per round and is the worst configuration measured. No
participation threshold is encoded anywhere. The standing protection is the
outcome-based gate `scripts/gate_collapse.py` (macro-F1 must clear the
closed-form constant-predictor baseline by 0.05 **and** every per-class F1 must
clear 0.05), which needs no guessed threshold.

Rebuild at operating point **K=20 / capacity=6 / R_comm=5 km / band 100-2000 m**,
PIPELINE_VERSION 5. **All four FL blocks landed 2026-08-10** (class realism,
fleet sweep, coverage sweep, paper_full); the Tier-1 rebuild is still queued.

### What the v5 data says, stated once (2026-08-10)

The corrected physics did not shrink the headline — it removed it. Across three
independent blocks the result is consistent and negative:

* **`proposed_hfl` does not beat the literature selectors.** In paper_full it
  Holm-loses to `fedcs`, `oort` and `flat_fl` and Holm-beats only the placement
  baselines and its own ablations (block 4 row).
* **No-UAV flat FL is the arm to beat, and mostly wins.** `flat_fl` ≈ 0.39-0.51
  depending on N; exactly one cell in the 102-cell coverage sweep beats it, and
  that cell is `moon2022` — a literature placement rule — not ours.
* **Class-aware *selection* is null; class-aware *placement* is the surviving
  effect** (+0.039, the only Holm survivor in block 1), which is the reverse of
  what the paper claimed.

The v4 numbers said the opposite because they were produced at 20 km with a
20-120 m altitude band, where coverage saturates at 99.95% and `flat_fl` is
starved of the coverage that the hierarchy gets for free. Three claims are now
unsupported and must not be restated: that the proposed selector beats FedCS/Oort/
PoC/rep-cap/fair-MAB, that the hierarchy beats flat FL by ~0.19, and that
class-aware selection is the contribution.

What is *not* established: that the method cannot work. The v6 evaluation tests a
specific, pre-registered mechanism (`REPORTS/preregistration_v6_method.md`) for
why placement underperforms. Until it reports, the honest position is the negative
one above, and every number quoted must come from a row below.

### Historical note (superseded, kept for the audit trail)

Before 2026-08-08 the paper's data was the 2026-07-29 final run, code commit
`9aa933ed`, results commit `e1894dc7` — every row dated 2026-07-29 below.
Everything dated before 2026-07-24 is smoke-scale or predates the literature
baselines / current configs. All of it is now void under Defect 1.

| Date | Command / config | Output file(s) | Git commit | Paper table/figure | Notes |
|---|---|---|---|---|---|
| 2026-06-25 | legacy `hflsim` standalone simulator (pre-`uavbench`) | `results/simulation_results_N14.csv`, `results/simulation_results_N35.csv` | `5386986` | none | Preliminary data only; produced by the legacy simulator, not the current harness. TODO: confirm nothing in the paper cites these; candidates for deletion. |
| 2026-06-29 | `uavbench run_sweep --config configs/tier2_sweep.yaml` (real data, subsample 0.1, N=30..250, methods incl. pso) | `results/tier2_sweep/sweep_rounds.parquet`, per-N `tier2_rounds.parquet`, `sweep_*.png` | `a4e9a07` | scalability figure (candidate) — TODO: confirm | Predates the 2026-07 seed-stream tag change, so not seed-comparable with reruns; regenerate before quoting. |
| 2026-06-29 | sweep smoke run, resolved config `tier2_sweep_synthetic` (`data.source: synthetic`, N=30/50, 2 rounds) | `results/sweep_smoke/*` | `5dc1360` | none (smoke) | Resolved config records `source: synthetic`; the current library rejects synthetic (real-data-only pipeline since 2026-07-14), so this cannot be regenerated as-is. |
| 2026-07-01 | `uavbench run_paper_sim`, resolved config `paper_full_system` (real data, **subsample 0.1**, N=100/200/500, **6 methods**, 3 seeds) | `results/paper_full/paper_sweep_rounds.parquet`, `results/paper_full/N{100,200,500}/seed*/fullsim_rounds.parquet`, `paper_*.png` (accuracy/F1 vs rounds, ablation table, comm/energy, scalability) | `6a9646d` | paper ablation table + accuracy/F1/comm figures (candidate) — TODO: confirm | Predates the literature baselines (`fedcs`/`rep_cap`/`fair_mab`, `mozaffari2016`/`alzenad2017`) and differs from the current `configs/paper_full.yaml` (subsample 1.0, 11 methods, N=30/50/100). Must be regenerated for submission. |
| 2026-07-08 (approx.) | `uavbench run_paper_sim` partial rerun at N=30/50 (6 methods, per-method subdirectories) | `results/paper_full/N{30,50}/seed*/<method>/fullsim_rounds.parquet` | `c3e00de` | TODO: confirm | Newer per-method directory layout than the N=100/200/500 run above; still 6 methods, so also superseded by the 11-method config. |
| 2026-07-14 | `scripts/reproduce_paper.sh --smoke` — Tier-1 step (`configs/smoke.yaml`: 5 methods x 2 scenarios x 5 seeds) | `results/smoke/{runs,convergence,summary}.parquet`, `seed_manifest.csv`, `significance.csv`, `convergence_*.png`, `config.resolved.yaml` | `5c55a7a` | none (smoke validation) | End-to-end pipeline check; log in `results/reproduce_paper.log`. |
| 2026-07-14 | `scripts/reproduce_paper.sh --smoke` — Tier-2 step (`uavbench smoke_tier2`, `configs/tier2_reduced.yaml`) | `results/tier2_reduced/tier2_rounds.parquet`, `confusion.parquet`, `tier2_*.png`, `config.tier2.resolved.yaml` | `5c55a7a` | none (smoke) | Resolved config records `data.source: synthetic` although the pipeline is documented real-data-only — same-day cleanup artifact. TODO: confirm and regenerate on the real path. |
| 2026-07-14 | `scripts/reproduce_paper.sh --smoke` — stress step (`uavbench run_stress_sweep` with a *temporary scratchpad* `stress_ci.yaml`: 2 cells x 2 methods x 2 seeds, synthetic) | `results/stress_smoke_ci/stress_rounds.parquet`, `seed_manifest.csv`, `significance.csv`, per-cell dirs, `config.stress.resolved.yaml` | `5c55a7a` | none (smoke) | The driving config is not checked in (`/tmp` scratchpad path in the log), so this run is not reproducible from `configs/`. Regenerate with `configs/stress_test.yaml`. |
| 2026-07-14 | `scripts/reproduce_paper.sh --smoke` — artifact staging step | `results/paper_artifact/*` (copies of the above manifests, resolved configs, significance CSVs) | `5c55a7a` | provenance bundle | Derived copies, no new data. |
| 2026-07-24 | `uavbench run/analyze/plot/significance --config configs/tier1_core.yaml` (**HEADLINE**: 7 methods x 3 scenarios x 30 seeds; binding capacity K·cap=150<N=250; equal-radius scoring, baselines at max_path_loss_db=95 → r*≈499≈R_comm; Bayesian fitness weights w=0.811/0.03/0.159) | `results/tier1_core/{runs,convergence,summary}.parquet`, `significance_final_fitness.csv` (Holm), `convergence_*.png`, `seed_manifest.csv`, `config.resolved.yaml` | `b9662bdc` | Tier-1 headline table + convergence figures | Definitive placement comparison. PSO significantly beats every non-optimizing baseline (mozaffari/alzenad/centroid/static/random) in all 3 scenarios (Holm p≤1.9e-9); beats GA on clustered (+0.029) & epicenter (+0.067); **ties GA on uniform** (−0.006, p=0.10). Weights derived by Optuna in `scripts/tune_weights.py` (objective: downstream `proposed_hfl` macro_f1; `results/hpo/`; strongly identified, coverage corr +0.78), applied identically to all methods — disclosed objective coupling. Run in an isolated worktree on the GCP VM. |
| 2026-07-24 | `uavbench …/tier1_equal_radius.yaml` (**saturating-capacity ablation**: cap=30 → 300 slots > N=250; else identical to tier1_core) | `results/tier1_equal_radius/{runs,convergence,summary}.parquet`, `significance_final_fitness.csv`, `convergence_*.png` | `b9662bdc` | ablation (regime robustness) | PSO still beats all non-optimizers in all 3 scenarios; GA sig-wins uniform (−0.009), PSO wins clustered/epicenter. Shows the ranking is not an artifact of the binding regime. |
| 2026-07-24 | `uavbench …/tier1_regime_hetero.yaml` (**heterogeneous-fleet supplement**: binding + per-UAV capacity_cv=0.5, battery_cv=0.3; 20 seeds) | `results/tier1_regime_hetero/{runs,convergence,summary}.parquet`, `significance_final_fitness.csv`, `convergence_*.png` | `b9662bdc` | supplement (fleet realism) | PSO beats all non-optimizers in all 3 scenarios; its edge over the value/capacity-blind physics baselines **widens** vs the homogeneous headline (e.g. vs mozaffari clustered +0.044→+0.062, epicenter +0.123). Ties GA on uniform. |
| 2026-07-24 | `uavbench …/tier1_warmprev.yaml` (**movement-robustness supplement**: saturating + prev_mode=warm; 20 seeds) | `results/tier1_warmprev/{runs,convergence,summary}.parquet`, `significance_final_fitness.csv`, `convergence_*.png` | `b9662bdc` | supplement (movement model) | PSO win robust to incremental repositioning: beats all non-optimizers in uniform & clustered; on epicenter beats all but **ties centroid** (+0.003, p=0.35 — concentrated demand + slack capacity + negligible movement make centroid near-optimal). Ties GA on uniform. |
| 2026-07-29 | **FINAL RUN** — `scripts/run_gcp.sh` → `scripts/reproduce_paper.sh` (full pipeline, 12 workers, GCP `instance-20260715-110613`; started 2026-07-27, finished 2026-07-29T13:12:29Z, ~50 h wall) | all `results/*` below | `9aa933ed` (code) → `e1894dc7` (results commit) | — | Single coherent run at one commit; every table/figure quoted in the paper comes from this run unless a row below says otherwise. VM `git push` failed (no GitHub credential on the VM); results transferred by `git bundle create /tmp/results.bundle 9aa933ed..main` + `scp` + fast-forward merge, then pushed from the laptop. |
| 2026-07-29 | `uavbench run/analyze/plot/significance --config configs/tier1_core.yaml` (**PLACEMENT HEADLINE**: 7 methods x **4 scenarios** x 30 seeds; 4th scenario `distribution: real` = KMeans centres of the actual Noto 2024 client coordinates; binding capacity K·cap=150<N=250; equal-radius scoring at `max_path_loss_db=95`; Optuna weights w=0.811/0.03/0.159) | `results/tier1_core/{runs,convergence,summary}.parquet`, `significance_final_fitness.csv` (Holm, + rank-biserial + bootstrap CI), `convergence_*.png`, `seed_manifest.csv`, `config.resolved.yaml` | `e1894dc7` | Tier-1 headline table + convergence figures | 71/84 comparisons Holm-significant. **PSO wins 22/24 of its own comparisons** (p≤3.7e-9 vs static/random/centroid/mozaffari2016/alzenad2017 in all 4 scenarios). The 2 non-significant are both PSO-vs-GA: `real` (GA −0.0076, p=0.035, fails Holm) and `uniform` (PSO −0.0062, p=0.096). Report as "PSO ≥ GA, strictly beats every non-optimizing baseline" — do **not** claim PSO > GA on unstructured geometry. Supersedes the 2026-07-24 tier1_core row (3 scenarios). |
| 2026-07-29 | `uavbench mclp_reference --config configs/tier1_core.yaml` (capacitated max-covering MILP via `scipy.optimize.milp`/HiGHS, 20x20 grid = 400 candidate sites, 4 scenarios x 3 seeds) | `results/tier1_core/mclp_reference.csv` | `e1894dc7` | near-optimality claim (text/table) | All 12 instances solved to proven optimality (`mclp_optimal=True`). **PSO coverage = 99.3% of the MILP optimum on average** (range 96.4–101.1%; >100% where PSO places off-grid, i.e. the MILP is a grid-restricted bound, not a true upper bound — state this caveat). Only 3 seeds/scenario: report as a spot-check, not a distribution. |
| 2026-07-29 | `uavbench run_paper_sim --config configs/paper_full.yaml` (**FL HEADLINE**: 13 methods x N=30/50/100/200 x 10 seeds, 100 rounds, subsample 1.0, R_comm=20 km, K=20, cap=6) | `results/paper_full/paper_sweep_rounds.parquet`, `N{30,50,100,200}/seed*/<method>/fullsim_rounds.parquet`, `significance_macro_f1.csv`, `operational_summary.csv`, `paper_*.png` | `e1894dc7` | main results table + accuracy/F1-vs-round figures + ablation table | `proposed_hfl` Holm-beats **all five literature selectors** (fedcs, oort, power_of_choice, rep_cap, fair_mab) and both structural baselines (flat_fl +0.19, hfl_no_selection +0.066) at N=30–100; at N=200 fedcs/PoC/flat_fl/no_selection stay significant but rep_cap/oort/fair_mab drop below Holm. Loses to `centralized` at every N (−0.030…−0.075) — that is the intended upper bound, report it as such. **Placement baselines (mozaffari2016, alzenad2017, hfl_static) are NOT significantly different** — consistent with the coverage sweep below. Convergence: `proposed_hfl` reaches macro_f1 0.45 in 25.6 rounds vs 46–70 for the selection baselines. |
| 2026-07-29 | `uavbench run_selection_sim --config configs/selection_isolation.yaml` (**SELECTION ISOLATION**: 7 selectors x N=30/50/100/200/350/500 x 10 seeds, 200 rounds, **subsample 1.0** = full-data variant, static elbow K-means UAVs so placement is held fixed; 45 h of the 50 h run) | `results/selection_isolation/N*/seed*/<selector>/selection_rounds.parquet`, `confusion.parquet`, `selection_sweep_rounds.parquet`, `significance_macro_f1.csv` (`--reference ucb --correction-scope group`), `selection_*.png` | `e1894dc7` | selection-isolation table + scalability figure | Selection rule is the **only** variable. UCB (the proposed selector) is Holm-significant over ≥1 baseline at every N≥50, and the margin **grows monotonically with N**: at N=500 it beats fedcs +0.048, oort +0.041, random +0.032, PoC +0.030, rep_cap +0.035, fair_mab +0.022 (all Holm-sig); at N=30 the sign is mixed and nothing is significant. Report the small-N regime honestly — the advantage requires selection pressure, it is not universal. Holm is applied **within each N block** (`correction_scope=group`), not across all 36 comparisons; state this. |
| 2026-07-29 | `uavbench run_selection_sim --config configs/selection_isolation_starved.yaml` (same grid, **subsample 0.1**) — retained as the data-starved contrast | `results/selection_isolation_starved/*` — superseded as the headline by the full-data variant above | `e1894dc7` | supplement (data-starvation control) | Kept deliberately: under 10% subsampling UCB's advantage **inverts at large N** (−0.063 at N=500), which is a data-starvation artifact, not a scaling failure of the selector — the matched-data rerun above flips it to +0.041. Use only to make that point; never mix the two variants in one table. |
| 2026-07-29 | `uavbench run_coverage_sweep --config configs/paper_coverage.yaml` (**COVERAGE-CONSTRAINED PLACEMENT**: R_comm ∈ {2,4,6,8,12,20} km at fixed N=200, 7 methods incl. naive `centroid_place`/`random_place` arms, 10 seeds; `uniform_coverage_radius: true`; path-loss calibration scoped to mozaffari2016/alzenad2017 only) | `results/paper_coverage/coverage_sweep_rounds.parquet`, `R{2000..20000}/`, `significance_macro_f1.csv`, `coverage_{macro_f1,accuracy,coverage_pct}.png` | `e1894dc7` | coverage-constrained figure + honest-limitation text | **NEGATIVE RESULT — the hypothesis was not confirmed.** Tightening R_comm does *not* make placement method separate on macro_f1: at every radius the five non-random placement methods are within ±0.02 and mostly non-significant, and at 4–6 km `proposed_hfl` is nominally *below* mozaffari2016/alzenad2017/centroid. Only `random_place` is materially worse, and only at R≤4 km. What the sweep *does* establish: (i) the HFL-over-`flat_fl` advantage is created by coverage and grows with it (+0.065 at 4 km → +0.190 at 20 km, Holm-sig at every R≥4 km; at 2 km it vanishes, −0.007 ns); (ii) PSO trades raw coverage for the weighted objective — at 2 km it covers 26.6% vs mozaffari's 35.1% yet matches it on macro_f1. Frame placement's contribution via the **operational** metrics (`operational_summary.csv`), not end-task accuracy. Do not claim placement quality drives accuracy. |
| 2026-07-29 | `scripts/e2e_centralized.py` (frozen ResNet-18 features vs end-to-end trainable backbone + fusion head, pooled training set, same held-out test) | `results/e2e_centralized/e2e_comparison.csv` | `e1894dc7` | frozen-feature validation (text/small table) | Frozen: macro_f1 0.4113 / acc 0.7535. End-to-end: macro_f1 0.5000 / acc 0.7616. **Frozen features retain 82.3% of end-to-end macro-F1** at ~1/25 the compute. Single run, centralized only — report as a validation of the modelling choice, not as a federated result, and disclose the 17.7% headroom the frozen cache gives up. |
| 2026-07-29 | `uavbench run_selection_sim --config configs/selection_scaling_full.yaml` (N=350/500 extension of the selection scalability sweep, 4 selectors x 10 seeds) | `results/selection_scaling_full/*`, `significance_macro_f1.csv`, `selection_scalability.png` | `e1894dc7` | scalability figure (large-N tail) | Extends the isolation sweep's N axis. |
| 2026-07-29 | `uavbench run_stress_sweep --config configs/stress_test.yaml` and `configs/stress_selection.yaml` (checkpoint-resumed from the 2026-07-23 grid) | `results/stress_test/*`, `results/stress_selection/*`, `significance_macro_f1.csv` | `e1894dc7` | robustness/stress table | Resumed, not recomputed — the underlying cells date from the 2026-07-23/24 runs at the same pipeline version. Weights come from `configs/tuned_weights.yaml` via `extends:` (never copied inline — a copied `lr` previously drifted 17x and wrecked the 2026-07-22 stress grid). |
| 2026-07-29 | artifact staging step of `reproduce_paper.sh` | `results/paper_artifact/*` (resolved configs, seed manifests, significance CSVs) | `e1894dc7` | provenance bundle | Derived copies, no new data. |
| 2026-08-07 | `uavbench run_coverage_sweep --config configs/paper_coverage.yaml` (**COVERAGE SWEEP v2** — supersedes the 2026-07-29 row above: **17 methods** x R_comm ∈ {250, 500, 1k, 2k, 4k, 8k, 20k} m x 10 seeds at N=200; shared Al-Hourani path-loss link model calibrated per job so the best achievable radius equals the swept R_comm; equal-radius re-scoring on a fresh canonical Fitness; PIPELINE_VERSION 4) | `results/paper_coverage/coverage_sweep_rounds.parquet`, `R{250..20000}/seed*/<method>/`, `significance_macro_f1.csv` | `60d20565` (results) on code `4e366c3f` | coverage-constrained figure + honest-limitation text | **NEGATIVE RESULT, re-confirmed at much higher resolution.** Paired Wilcoxon vs `mclp_place`, Holm within radius: **6 of 105 placement-vs-placement comparisons significant.** vs `random_place`: positive at all 7 radii (+0.006…+0.067) but **none survives Holm** — consistent sign (7/7) suggests a small real effect, magnitude does not clear multiplicity at 10 seeds; do **not** claim a significant win over random. vs `hfl_static` (placed once, never repositioned): \|diff\| ≤ 0.03, all p > 0.6 — **repositioning buys no accuracy.** vs `proposed_hfl` (PSO): \|diff\| ≤ 0.027, all ns. The 6 wins that land are `alzenad2017`/`moon2022` at 20 km (+0.20) and `ga_place` at 500 m — methods whose own altitude rule mismatches the link budget, i.e. coverage *failures*, not placement-quality differences. `flat_fl` significantly **beats** every placement at R ≤ 2 km (−0.19…−0.10) and loses at 20 km (+0.138). Why the 2026-07-29 row does not carry over: that sweep scored each optimizer at its own path-loss radius (618 m for mozaffari/alzenad vs R_comm for everyone else), so its `placement_fitness` column was not comparable across methods. |
| 2026-08-08 | `scripts/run_class_realism.sh` → `uavbench run_paper_sim` x 4 arms (**ORACLE-DEGRADATION ABLATION**, rigor plan item 0.5: `proposed_hfl` at N=200, R_comm=20 km, 10 seeds, 100 rounds; arms vary `fl.class_source` ∈ {true, pseudo, none} x `fl.placement_class_aware` ∈ {on, off}; all arms share `optimizer_seed`/`data.seed`/method so pairing is exact) | `results/class_realism_{true,pseudo,noplace,none}/paper_sweep_rounds.parquet`, merged `results/class_realism/paper_sweep_rounds.parquet`, `significance_{macro_f1,accuracy,f1_*}.csv` | pending bundle, on code `4e366c3f` | class-awareness realism table | Means: `true_placeaware` 0.5262, `pseudo_placeaware` 0.5166, `true_placeblind` 0.5250, `none_placeblind` 0.4538. Paired Wilcoxon vs the oracle arm (Holm): **vs `none_placeblind` +0.0653, p=0.0059, rank-biserial 0.93, CI [0.034, 0.097] — class-awareness is doing real work.** vs `pseudo_placeaware` +0.0100, p=0.131, CI [−0.001, 0.022] — **not significant**; pseudo retains ≈87% of the benefit with **zero label disclosure**, so report `pseudo` as the operating point and `true` as an upper bound. vs `true_placeblind` +0.0035, p=0.77 — null, **but this contrast is powerless and must not be cited as evidence**: coverage is 99.95% at 20 km, so class-aware placement has nothing left to steer (see the 8 km row). Side finding: at saturated coverage class-aware placement burns **41% more movement energy** (1.129e8 vs 7.997e7 J) for +0.0012 macro-F1 and slightly worse accuracy. Prerequisite fix: `class_source: pseudo` was **unreachable** from `run_full_hfl` before this run (it called `build_class_info` without a model and raised on every invocation); it now refreshes from the current global model on the reselection cadence, ahead of placement. **VOID under Defect 1** (ran at 20 km / 120 m). The ablation *design* is sound and is being re-run at the corrected operating point; the numbers are not. Note the absolute level drops from 0.526 to ~0.38 in the corrected regime, so whether the +0.065 selection effect survives is an open question. |
| 2026-08-08 | `uavbench run_paper_sim` × 3 attempts at 2 km (K=20/cap=6 PSO; K=60/cap=2 PSO; K=60/cap=2 mclp_ls) | overwritten by later runs; numbers preserved in `src/uavbench/analysis/collapse.py` and `tests/sanity_checks/check_collapse_guard.py` | `16a52ddd`…`b19cf292` | **none — failed runs** | Kept in the record because they are the calibration set for the collapse gate. macro-F1 0.287 / 0.266 / 0.262 against a 0.2245 constant-predictor floor, with `f1_missing` 0.0040 / 0.0062 / 0.0016. All three produced complete, fully-checkpointed, plausible output; every sanity check passed. Caught only by reading `f1_missing` by hand, which is why the gate now exists. |
| 2026-08-09 | `uavbench run_uav_sweep --config configs/probe_topology.yaml` (**CAPACITY DIAGNOSTIC**, 6 (K,capacity) cells × 1 method (`mclp_place`) × 3 seeds, N=200, R_comm=5 km, band 100-2000 m, 100 rounds) | `results/probe_topology/uav_sweep_rounds.parquet`, `K*/seed*/mclp_place/` | `9349f274` | **diagnostic — not paper material at 3 seeds** | Established Defect 2. Final macro-F1 by cell: K=120/cap=1 **0.199** (below the 0.2245 floor), K=60/cap=2 0.274, K=40/cap=3 0.286, K=30/cap=4 0.379, **K=20/cap=6 0.395**, K=60/cap=6 0.367 (off-ladder control, 360 slots). Coverage across the grid 82.3-98.2% and does **not** order the outcome; capacity does, exactly. Failure mode is unlearning, not starvation — read the trajectory, not the final row. Fixes the operating point at K=20/cap=6 and forced the fleet sweep's K·C=120 invariant to be **inverted** (capacity now fixed at 6, slots vary), because the old grid's entire high-K half sat in the unlearning regime and would have reported capacity collapse as a fleet-size effect on placement. |
| 2026-08-09 | `scripts/run_rebuild_v5.sh` (**V5 REBUILD**, in progress; 4 blocks: class realism ×6 arms → fleet sweep → coverage sweep → paper_full; every block gated by `scripts/gate_collapse.py`, first-arm collapse aborts the whole run) | `results/class_realism_*`, `results/paper_uav_count`, `results/paper_coverage_v5`, `results/paper_full`, log `results/rebuild_v5.log` | code `eaef0174`+ | **pending** | Operating point K=20/cap=6, R_comm 5 km, band 100-2000 m, PIPELINE_VERSION 5 (refuses v4 checkpoints; all 10 class-realism seeds correctly invalidated on `K: 60→20, capacity: 2→6`). Arms cleared so far: `class_realism_true` macro-F1 0.3837 (margin +0.159, min per-class F1 0.155), `class_realism_pseudo` 0.3385 (min 0.065), `class_realism_noplace` 0.3432 (min 0.143). Do not quote until the block completes and its significance tables are written. |
| 2026-08-09 | v5 rebuild **block 1 — CLASS-REALISM ABLATION** (6 arms: 4 at R_comm 5 km + a 3 km bind pair; `proposed_hfl`, N=200, K=20, cap=6, 10 seeds, 100 rounds) | `results/class_realism_{true,pseudo,noplace,none,bind,bind_noplace}/`, merged `results/class_realism_{main,bind}_merged/`, `significance_*.csv` | code `697e1dfa`+ | class-awareness table — **claim changed, see notes** | Arm means: `true_placeaware` 0.3837, `true_placeblind` 0.3432, `pseudo_placeaware` 0.3385, `none_placeblind` 0.3185. Paired Wilcoxon on the **mean of the last 10 rounds per seed** (the shipped reduction — reproduced to 1e-9; the final-round convention gives materially different p-values, so state which is used). **PLACEMENT only** (selection true in both) **+0.0391, p=0.0137, CI [+0.015, +0.063] — the only contrast surviving Holm.** BOTH +0.0564, p=0.0371 (fails Holm). **SELECTION only +0.0173, p=0.375, CI [−0.017, +0.052] — null.** pseudo vs none +0.0117, p=0.557 — null. **This inverts the 2026-08-08 result** (selection +0.071 p=0.006, placement null), which was a saturation artifact: at 20 km coverage was 99.95% so placement had nothing to steer. Two claims the paper can no longer make: "class-aware *selection* is the contribution", and "pseudo-labels retain ~87% with no disclosure". Do not over-read the nulls — n=10 and the selection CI is wide enough to contain a useful effect; they are "not detected", not "absent". The 3 km bind pair gives placement −0.0083 (p=0.70), so the effect is **not** monotone in radius and the dose-response prediction failed. |
| 2026-08-09 | v5 rebuild **block 2 — FLEET SWEEP** (`run_uav_sweep`, K ∈ {5,10,15,20,30} × capacity 6 fixed × 17 methods × 10 seeds, N=200, R_comm 5 km, 100 rounds) | `results/paper_uav_count/uav_sweep_rounds.parquet`, `K*/seed*/<method>/`, `significance_*.csv` | code `697e1dfa`+, results `15c25303` | fleet-size figure — **two negative results, see notes** | Gate: **6/85 cells degenerate**, all of them `random_place`/`mozaffari2016`/`mogoa` at K=5 and K=10 — i.e. the worst-coverage rules at the smallest fleets, which is a result rather than a fault. Validity check passed (`flat_fl` identical across all five K). **(a) `flat_fl` (no UAVs, 0.3929) beats the proposed placement at every K.** In v4 the proposed method beat it by +0.19; that advantage was an artefact of the 20 km radius — `flat_fl` scores ~0.38-0.39 in both v4 and v5 because it does not depend on radius, while the HFL system fell from 0.565 to ~0.37. **(b) `moon2022` (literature) beats `mclp_place` at every K** (0.4122/0.4181/0.4089/0.4061 vs 0.3910/0.3850/0.3739/0.3667 at K=10/15/20/30). Also: an inverted-U in K — `mclp_place` peaks at K=10 and declines to K=30 while coverage keeps rising, so more aircraft eventually hurt. Note the 2026-08-06 evidence that made `mclp_ls` the proposed placement ("649/650 wins, 97-99% of optimum") predates the altitude fix and is void on the same grounds as everything else. |
| 2026-08-10 | v5 rebuild **block 3 — COVERAGE SWEEP** (`run_coverage_sweep`, R_comm ∈ {500, 1k, 2k, 3k, 4k, 5k} m × 17 methods × 10 seeds, N=200, K=20, cap 6, 100 rounds) | `results/paper_coverage_v5/coverage_sweep_rounds.parquet`, `R*/seed*/<method>/`, `significance_*.csv` | code `697e1dfa`+ | coverage-constrained figure + operational-limit text | Gate: **47/102 cells degenerate**, spanning every method at 500 m-1 km and reaching 2-3 km for the weak-coverage rules — expected and documented in the config beforehand, not a surprise. **Operational finding worth stating: below ~3 km a 20-UAV fleet cannot learn this task at all.** macro-F1 rises monotonically with radius for every method. **Across all 102 cells exactly one beats `flat_fl` (0.3929): `moon2022` at 5 km (0.4089), at 93.4% coverage**, where `mclp_place` reaches 80.4% and 0.3739. So the UAV system *can* beat flat FL, but only near coverage saturation — which is precisely what the v6 C1 objective change targets. **Limitation:** the curve is still rising at 5 km and 5.4 km is the coherent ceiling for the 100-2000 m band, so no further coverage is available from radius inside the current physics. |
| 2026-08-10 | v5 rebuild **block 4 — PAPER_FULL (FL HEADLINE)** (`run_paper_sim --config configs/paper_full.yaml`: 13 methods × N ∈ {30,50,100,200} × 10 seeds, 100 rounds, N=200 clients over K=20 UAVs at cap 6, R_comm 5 km, band 100-2000 m, subsample 1.0) | `results/paper_full/paper_sweep_rounds.parquet`, `N*/seed*/<method>/`, `significance_macro_f1.csv`, `operational_summary.csv`, `per_class_f1.csv`, `paper_*.png` | code `697e1dfa`+, results `ed657e54` | main results table — **the paper's central claim does not survive; see notes** | Gate: **6/52 cells fail, in two distinct ways that must not be merged.** `alzenad2017` N=30/50 and `mozaffari2016` N=30 sit within 0.05 of the 0.2243 floor (collapse proper); `hfl_no_reputation` N=50, `hfl_no_selection` N=30 and `mozaffari2016` N=50 clear the macro-F1 margin but leave a rare class under F1 0.05 (`f1_missing` 0.022/0.043, `f1_collapsed` 0.045) — "never learned the rare class", not "predicts the majority". (The gate reads the final round; every number below is the shipped mean-of-last-10-rounds reduction, so cell values differ by ~0.01 from the gate's.) **`proposed_hfl` does not Holm-beat a single literature selector anywhere in the grid, and loses to three of them.** Holm within N, reference `proposed_hfl`, 19/48 significant: N=30 loses to fedcs −0.079, flat_fl −0.168, centralized −0.253. N=50 loses to oort −0.105, fedcs −0.111, flat_fl −0.139, centralized −0.289. N=100 **beats** alzenad2017 +0.099, rep_cap +0.082, mozaffari2016 +0.065, hfl_no_selection +0.059 but **loses** to fedcs −0.035, oort −0.050, flat_fl −0.052. N=200 beats alzenad2017 +0.090, mozaffari2016 +0.048, loses to oort −0.052. The pattern is exact: every win is over a *placement* baseline or over its own ablation; every loss is to a *selection* baseline or to no-UAV flat FL. **This inverts the 2026-07-29 row**, which reported `proposed_hfl` Holm-beating all five literature selectors at N=30-100. Best federated arm is `flat_fl` at N=30/50/100 (0.510/0.449/0.433) and `oort` at N=200 (0.432); `centralized` holds 0.595-0.599 at every N. Regime note: at N=200, K·cap=120 < 200, so coverage falls to ~81% for every selector — that column is capacity-bound, not a scaling result. |
| 2026-08-10 | **shard-width hypothesis test** (analysis only, no new compute: `mean_shard_clients` vs `macro_f1` over the 80 hierarchical cells of block 2's fleet sweep) | derived from `results/paper_uav_count/uav_sweep_rounds.parquet` | code `697e1dfa`+ | **diagnostic — refuted hypothesis, kept so it is not reintroduced** | paper_full shows every hierarchical arm at `mean_shard_clients` 1.03-2.96 — below the width-3 unlearning floor of the 2026-08-09 capacity probe — while `flat_fl`, whose single shard holds 9.2-59.8 clients, beats them all. The obvious inference is that shard width is the controlling variable and the selection policy is incidental. **It is wrong.** Across the fleet sweep's 80 hierarchical cells, Pearson r(width, macro-F1) = **0.117**. The counter-examples are flat: `mogoa` has the widest shards in the entire grid (5.43 at K=5) and the second-worst macro-F1 (0.230); `random_place` at width 4.45 scores 0.218, while `moon2022` at width 3.23 scores 0.409. So the capacity-floor result stands **only as a floor effect within one placement method** (the probe varied capacity alone), and does not generalise to a cross-method explanation. Widening shards is therefore not a fix for the block-4 result, and no operating point should be chosen on width. |
| 2026-08-10 | **v6 arm C1 — reachable-coverage placement** (`run_uav_sweep --config configs/v6_c1_reachable.yaml`: `mclp_place` only, K ∈ {5,10,15,20,30} × 10 seeds, N=200, cap 6, R_comm 5 km; baselines reused from `results/paper_uav_count` at identical N/seeds/rounds, which is what keeps the pairing valid) | `results/v6_c1_reachable/uav_sweep_rounds.parquet`, control in `results/v6_control/` | code `697e1dfa`+ | v6 verdict (arms C2 / C1+C2 still running) | **Control first: both switches off reproduces v5 `mclp_place` at K=20 with diff = 0.0 on all 3 seeds — exact, so the arms measure the method and not the refactor.** **C1's mechanism fired as pre-registered:** coverage_pct rises at every K — 29.7→31.3 (K=5), 56.8→65.0, 69.1→80.1, 80.4→88.8, 93.9→95.6 — i.e. +8 to +11 pp through the middle of the grid, toward `moon2022`'s 65.9/83.0/93.4. **The accuracy did not follow.** Paired per K vs v5 `mclp_place`: +0.020 (K=10, p=0.19), +0.005 (K=20, p=0.85), −0.002/−0.006/+0.003 elsewhere — significant at no K. Against the pre-registered criteria C1 alone **fails criterion 1** (0 of 3 wins vs `moon2022`; loses K=15 −0.040 p=0.006 and K=20 −0.030 p=0.027) and **fails criterion 2** (negative at K=5 and 15); passes criterion 3 (vs `flat_fl` at K=20, −0.014 p=0.16) and criterion 4 (gate cleared all 5 cells). Note C1 also *raised* shard width (2.85→2.95 at K=20, 3.75→4.25 at K=10) with no accuracy gain — a third strike against the width hypothesis. |
| 2026-08-10 | **coverage-causality test** (analysis only: the observational cross-method slope of macro-F1 on coverage_pct, fitted per K over the 16 non-`flat_fl` methods of the fleet sweep *excluding* C1, used to predict C1's interventional gain) | derived from `results/paper_uav_count/` + `results/v6_c1_reachable/` | code `697e1dfa`+ | **methodological finding — strongest positive result currently in hand** | Across methods at fixed fleet size, `coverage_pct` is by far the best mediator of macro-F1: within-K r = **0.70 mean** (0.493/0.803/0.838/0.798/0.577 at K=5/10/15/20/30), positive at every K, ahead of `n_active_uavs` 0.63, `shard_class_entropy` 0.42, `mean_shard_clients` 0.39, `jain_fairness` 0.37, `n_unique_selected` 0.35 (the last three flip sign at some K). **C1 is a direct intervention on that variable and breaks the association: summed over K it delivers 34% of the slope's prediction (+0.019 realised vs +0.056 predicted), and the per-K realised fraction is −0.31 / 0.88 / −0.36 / 0.56 / 2.38 — not merely attenuated, but inconsistent in sign.** So the r=0.70 cross-method correlation is largely confounded, and placement work that optimises a coverage proxy — including this project's own prior framing — is optimising something that does not transfer to end-task accuracy. Second result from the same fit: `moon2022`'s advantage is **not** explained by its coverage. Its residual off the per-K line is +0.019 (rank 6/16), −0.005 (11/16), +0.016 (5/16), +0.022 (**2/16**), +0.042 (**1/16**) at K=5…30 — rising with fleet size, and at K=30 both it and `mclp_place` are at 94-100% coverage, so whatever it is doing there is coverage-independent. That residual is the open mechanism and the best remaining candidate for a positive contribution. |
| 2026-08-10 | **v6 full evaluation — C1 x C2 2×2** (`scripts/run_v6.sh`: control + `v6_c1_reachable` + `v6_c2_diversity` + `v6_both`, each `mclp_place` over K ∈ {5,10,15,20,30} × 10 seeds; baselines reused from `results/paper_uav_count`) | `results/v6_{control,c1_reachable,c2_diversity,both}/`, `results/v6_verdict.txt`, log `results/v6.log` | code `697e1dfa`+ (verdict relabelled at `fe2083d6`) | **v6 verdict — NEGATIVE, pre-registered** | Control reproduces v5 `mclp_place` at K=20 with **diff = 0.0** on all 3 seeds, so the arms measure the method rather than the refactor. **No arm meets the criteria in `REPORTS/preregistration_v6_method.md`; every arm fails criterion 1.** **C1** (reachable coverage): fails 1 and 2; its only Holm-significant result vs `moon2022` is a **loss** at K=15 (−0.0395). **C2** (diversity-weighted edge aggregation): the best arm — passes 2, 3, 4 and fails only 1. Positive vs `mclp_place` at every K (+0.002/+0.033/+0.022/+0.012/+0.016 at K=5/10/15/20/30) and nominally above `flat_fl` at K=10 (+0.031) and K=15 (+0.014), but **nothing is Holm-significant**: at K=10 the raw p-values 0.037/0.232/0.037 adjust to 0.111. Validity check for C2 passes — coverage moves ≤0.6 pp at every K, confirming it changes aggregation only, not placement. **C1+C2 is not additive and is mostly worse than C2 alone** (K=5 −0.008 vs `mclp_place` where C2 is +0.002; K=10 +0.017 where C2 is +0.033), though it is the only arm nominally *above* `flat_fl` at K=20 (+0.0030) and its best cell is K=20 vs `mclp_place` (+0.0220, raw p=0.0137). Reading: the reachable objective and the diversity gate interact through capacity — C1 spreads the fleet to reach more clients, which at fixed capacity thins the shards that C2 needs to be class-rich. Report as a negative result per the pre-registration; C2 is a direction worth more seeds, not a claim. |
| 2026-08-10 | **Tier-1 rebuild — FIRST ATTEMPT VOID** (`scripts/run_tier1_v5.sh`; aborted at the headline config) | `results/tier1_core/` (re-committed stale, `40de90ed`), log `results/tier1_v5.log` | code `697e1dfa`+ | **none — invalid run, kept as the record of a caught defect** | The run logged `Resuming: 840/840 jobs already checkpointed, running the remaining 0` and finished in **eleven seconds**, re-serving results computed 2026-08-06 under the old flat range gate and the old 20-120 m band. Cause: the Tier-1 checkpoint key was `(method, scenario, seed)` and encoded nothing about the physics, so changing `area.z` and `problem.link_model` — the entire point of the rebuild — invalidated nothing. `scripts/gate_altitude.py` caught it (`no mean_altitude_m column — this run predates altitude reporting`) and the script aborted before spending the three ablations on it. **Note what that means: the backstop was a gate specific to altitude. A fitness-weight change would have produced a clean, wrong, plausible table.** Fixed at `ca113e25`: checkpoints are now keyed by `runner._config_signature` over `area/problem/fitness/budget/value/scenarios/optimizer_params/instance_seed/optimizer_seed`, mismatched checkpoints are logged and ignored rather than silently reused, and `tests/sanity_checks/check_tier1_resume.py` (12 checks) asserts the key moves for each of those and does *not* move for cosmetic edits. Re-run queued. |
| 2026-08-09 | `scripts/run_tier1_v5.sh` (**TIER-1 REBUILD**, queued — not yet run) | `results/tier1_{core,equal_radius,regime_hetero,warmprev}` | code `5bc189fd` | **pending** | Tier-1 ran on a flat `slant_distance <= R_comm` gate until 2026-08-09, under which altitude is a pure penalty and every optimizer drives z to the floor: the advertised 3D search had a closed-form third dimension. Now scored through the shared Al-Hourani channel (`problem.link_model: path_loss`), band 100-400 m, R_comm 500 m. Two silent hazards closed in the same change: `compute_metrics` built its own link-less `Fitness` (so reported coverage would have used the flat gate for channel-chosen positions), and an explicit `radii` argument overrides the link (so forwarding a baseline's `meta["radii"]` would have rebuilt the 618-vs-500 m unequal-radius comparison). Under a shared channel no method carries a private radius — this is the structural fix for that fairness problem, not a patch. New `mean_altitude_m`/`std_altitude_m` columns; `scripts/gate_altitude.py` fails a run in which *every* method pins to the same bound. Held back deliberately: bash reads scripts lazily, so this could not be appended to the running rebuild. |
